"""Pytest configuration.

Importing this file places the repository root on ``sys.path`` so that tests
can import top-level modules (``config``, ``inference``, ``benchmark``) when the
suite is invoked as bare ``pytest`` from the project root, matching CI.
"""
